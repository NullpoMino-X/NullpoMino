version: '{build}'
os: 
  - Windows Server 2012
  - ubuntu
install:
  - cmd: java -version
  - sh: sudo apt-get update
  - sh: sudo apt-get install icedtea-netx -y
before_build:
  - cd nullpomino-parent
build_script:
  - mvn install
