version: '{build}'
os: 
  - Windows Server 2012
  - ubuntu
install:
  - sh: sudo apt-get update
  - sh: sudo apt-get install icedtea-netx maven
before_build:
  - cd nullpomino-parent
build_script:
  - mvn install
